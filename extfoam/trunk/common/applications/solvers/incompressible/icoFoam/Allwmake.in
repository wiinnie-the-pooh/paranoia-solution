#!/bin/sh
cd ${0%/*} || exit 1    # run from this directory

if [ "x@boost_ok@" = "xyes" ]; then
    set -x
    ( cd lib && wmake libso )
    wmake exe bin
fi


# ----------------------------------------------------------------- end-of-file
