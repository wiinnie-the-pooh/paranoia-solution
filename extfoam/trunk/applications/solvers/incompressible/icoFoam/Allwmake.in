#!/bin/sh
cd ${0%/*} || exit 1    # run from this directory

if [ "x@ENABLE_BOOST_SHARED_PTR@" = "xyes" ]; then
    set -x
    ( cd lib && wmake libso )
    wmake exe bin
fi


# ----------------------------------------------------------------- end-of-file
